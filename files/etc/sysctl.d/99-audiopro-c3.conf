# This is a speaker, not a router - it has no business forwarding packets between
# the setup AP and the home LAN. Set here because firewall4/nftables is not installed
# (its only live rule was forward REJECT; every input zone was ACCEPT).
net.ipv4.ip_forward=0
