# zram is the only swap here and it is RAM-backed, so pushing pages out costs an
# lzo compress instead of a flash write - cheaper than letting the kernel drop
# page cache that librespot's decode loop is about to read again.
vm.swappiness=100
